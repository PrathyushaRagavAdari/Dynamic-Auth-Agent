# Execution Instructions
GraphGuard uses a single-command startup script for reliable execution.

**Standard Execution**
Run the startup script, which handles dependency installation, smoke testing, and app launch:
```bash
bash run.sh